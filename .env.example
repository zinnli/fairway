# 로컬 백엔드 (docker compose up -d)
VITE_API_BASE=http://localhost/api/v1

# Vercel 임시 도메인 · fairway.click 연결 후 — 둘 다 같은 값
# VITE_API_BASE=https://api.fairway.click/api/v1

# mock | http — 목은 브라우저 안에서만 돈다 (백엔드 없이 시연할 때)
VITE_API=mock
