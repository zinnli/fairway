# 기능명세서 (v2)

> v2 (2026-08-25). 기획자의 기능명세서 마인드맵(5개 묶음)을 바탕으로, PRD·화면 시안·유저플로우와 맞춰 다시 정리했다.
> 제출용 기능명세서 PDF(양식 필수)는 이 문서를 양식 칸에 옮겨 적으면 된다.
> 화면 이름(S0~S6)은 02_IA_화면지도.md, 흐름(F0~F6)은 03_유저플로우.md와 같다.
>
> **2026-09-03 범위 축소.** 표 끝의 `제외(9/3)` / `축소(9/3)` 표시가 이번에 만들 것을 정한다.
> 무엇을 왜 뺐는지는 `04_기획축소_0903.md`. 이 문서와 `04`가 어긋나면 **`04`가 이긴다.**

## 읽는 법

- 번호는 기획자 마인드맵의 묶음을 그대로 따른다: **1** 사건 만들기 / **2** 분석·판정 / **3** 사건경위서 / **4** 반박의견서 / **5** 진행 상태·이력. 여기에 **0** 공통 규칙과 **6** 로그인을 더했다.
- 우선순위: **P0** = 시연이 되려면 꼭 / **P1** = 되면 좋음 / **P2** = 이번엔 안 함.
- **확인 문장** = 이 기능이 다 됐는지 눈으로 확인하는 기준. 제출용 PDF의 "수용 기준" 칸에 그대로 쓴다.
- **제외(9/3)** = 이번에 만들지 않는다 / **축소(9/3)** = 만들되 일부를 뺀다 (`04` 참고).
- ★ = 팀이 확정해야 하는 것.

---

## 0. 공통 규칙 (모든 화면에 적용)

| 번호 | 규칙 | 내용 |
|---|---|---|
| 0.1 | 비율 표기 | 항상 **"나 ○ : 상대 ○"** 순서. 숫자만 쓰지 않는다. (시안에서 같은 사건이 100:0과 0:100으로 다르게 나온 어긋남을 이 규칙으로 통일) |
| 0.2 | 참고용 고지 | "본 결과는 참고용이며, 최종 과실비율은 보험사·분쟁심의위원회 결정에 따릅니다." — 판정·서류가 보이는 곳 하단 고정 |
| 0.3 | ~~출처 태그~~ | **제외(9/3)** — 사실을 항목으로 다루지 않으므로 태그도 없다. 경위서 전문의 절별 근거 줄도 뺀다(9/4) |
| 0.4 | 영상 보관 ★ | 영상은 **그 사건 안에서만** 보관·사용한다(메일 첨부에 필요). 8/23 기획의 "분석 후 저장 안 함" 문구는 이렇게 바꾼다: "영상은 이 사건 처리에만 쓰이며, 사건을 지우면 함께 지워집니다" |
| 0.5 | 사건 상태 | 목록 배지: 접수중 → 분석중 → 확인 필요(질문 중) → 판정 완료 → 발송 완료 → 종결. **축소(9/3)** — "재판정중" 제거 |
| 0.6 | 진행 단계 | 사건 분석 · 과실비율 · 사건경위서 · 반박의견서 × (대기 / 진행중 / 완료) |
| 0.7 | 오류 문구 | 무엇이 안 됐는지 + 어떻게 하면 되는지 + [다시 시도]. 세 가지가 항상 같이. **축소(9/3)** — 발송 실패(4.3)에만 쓴다. 업로드·분석·PDF는 실패 경로를 만들지 않는다 |
| 0.8 | 빈 상태 | 사건 없음: "아직 사건이 없어요. 첫 사건을 만들어 보세요." ~~/ 이력 없음~~ (이력은 제외) |

## 1. 사건 만들기와 자료 올리기

| 번호 | 기능 | 하는 일 | 들어오는 것 → 나가는 것 | 규칙·예외 | 확인 문장 | 화면 | 우선순위 |
|---|---|---|---|---|---|---|---|
| 1.1 | 새 사건 만들기 | 빈 사건을 만들고 채팅을 연다 | [+ 새 사건] 클릭 → 사건 생성, 안내 카드 | 제목은 분석 뒤 AI가 자동으로 붙임(예: "교차로 직진 충돌 · 08-22"). 이름 바꾸기는 P1 | 새 사건을 누르면 3초 안에 채팅이 열리고 안내 카드가 보인다 | S3→S4 | P0 |
| 1.2 | 사고 설명 입력 | 사고 상황을 글로 받는다 | 글 → 채팅에 남고 AI가 응답 | 빈 입력은 보내기 버튼 비활성 | 입력한 설명이 채팅에 남고 AI가 답한다 | S4-A | P0 |
| 1.3 | 영상 올리기 | 블랙박스 영상을 받고 검사한다 | 파일 → 통과 시 업로드, 실패 시 이유 | mp4 권장(avi·mov 허용), 최대 200MB·3분 ★수치 확정 필요. 올리는 동안 진행 표시. **축소(9/3)** — 취소·오류 예외처리를 만들지 않는다(사진 첨부도 제외) | 영상을 고르면 진행 표시가 보이고 끝나면 분석이 시작된다 | S4-A | P0 |
| 1.4 | 분석 자동 시작 | 설명+영상이 모이면 분석을 시작한다 | 업로드 완료 → 분석 중 카드, 상태 "분석중" | 버튼 없음. 설명이 없으면 먼저 요청 | 업로드가 끝나면 버튼을 누르지 않아도 분석 중 카드가 보인다 | S4-A | P0 |
| 1.5 | 사건 목록 | 내 사건을 최신순으로 보여 준다 | — → 카드(제목·날짜·상태 배지) | 빈 상태 문구(0.8). PC는 사이드바 상시 | 사건이 2개 이상일 때 각 사건의 상태 배지가 다르게 보인다 | S3 | P0 |

## 2. 사고 분석과 과실비율 판정

| 번호 | 기능 | 하는 일 | 들어오는 것 → 나가는 것 | 규칙·예외 | 확인 문장 | 화면 | 우선순위 |
|---|---|---|---|---|---|---|---|
| 2.1 | 영상에서 사실 뽑기 (AI) | 영상을 보고 무엇이 있었는지 **글로** 정리한다 | 영상(+설명) → **요약 텍스트** | **축소(9/3)** — 사고마다 볼 것이 달라 항목 틀로 못 박지 않는다. 실패 경로도 만들지 않는다 | 분석이 끝나면 영상에서 본 내용이 글 한 덩이로 보인다 | S4-A | P0 |
| ~~2.2~~ | ~~사실 확인·고치기~~ | — | — | **제외(9/3)** — 사실 카드(h18)·고치기(h19·h23)를 모두 뺀다 | — | — | **제외** |
| 2.3 | 부족한 정보 묻기 (AI) | 판정에 모자란 것을 하나씩 묻는다 | 모자란 것 → **텍스트 질문** | **축소(9/3)** — 선택 칩·사진 첨부 없이 글로만 묻고 글로만 받는다. 한 번에 한 질문 | 필요한 것이 모자라면 판정 대신 질문이 이어진다 | S4-A | P0 |
| 2.4 | 예상 과실비율 내기 (AI) | 인정기준 도표로 기본 비율을 찾고 사정을 더하고 뺀다 | 분석 요약 → 비율, 도표 이름, 가감 목록, 한 줄 결론 | 근거 없는 숫자 금지. **축소(9/3)** — 도표 팝업(P-2)과 쟁점 줄을 뺀다. 도표는 근거 목록의 글 한 줄로만 남는다 | 판정 카드에 비율·한 줄 결론·근거 목록이 함께 보인다 | S4-A | P0 |
| 2.5 | 유사 심의사례 붙이기 (AI) | 비슷한(특히 뒤집힌) 사례를 찾아 붙인다 | 분석 요약 → 사례 2~3건(번호·한 줄) | 뒤집힌 사례 우선. 누르면 팝업(P-1). **축소(9/3)** — 일치도(%) 배지를 빼고 팝업 내용도 글로만 | 사례를 누르면 요약 팝업이 열린다 | S4-A | P0 |
| ~~2.6~~ | ~~상대 주장과 비교~~ | — | — | **제외(9/3)** — 입력 화면(h22)·비교 줄·차이 %p 안내를 모두 뺀다 | — | — | **제외** |
| ~~2.7~~ | ~~재판정~~ | — | — | **제외(9/3)** — 고칠 사실이 없으니 다시 판정할 계기도 없다 (h24·h25 제외) | — | — | **제외** |

### ~~2.x 화면의 "확인된 사실 N / 6"이 세는 것 (8/27 확정)~~ — **9/3 폐기**

> 사실을 항목으로 세지 않기로 했다. 아래는 8/27 기록으로만 남긴다.

- **분모 6 = F2가 정한 필수 항목 6가지**뿐이다. 사고 날짜·장소와 AI가 정한 쟁점은 **분모에 넣지 않는다.**
- 시연 사례의 6가지: 내 차선 · 상대 진입 방향 · 상대 신호 · 내 속도 · 충돌 부위 · 정지선 통과 시점.
  (이 사고에서는 쟁점인 "정지선 통과 시점"이 필수 6번째 자리를 차지한다. 사고 유형에 따라 6번째는 달라질 수 있다.)
- 아직 못 채운 항목은 **[확인 필요]** 상태로 분모에는 들어가고 분자에서는 빠진다.
  그래서 화면 문구는 **"확인된 사실 5 / 6 · 남은 1개는 쟁점이에요"** 한 벌로 쓴다.
- **영상을 올리기 전에는 분모를 미리 보여 주지 않는다.** "영상을 올리면 여기에 쌓여요."로 대신한다.
  분석 중에는 그때까지 읽어 낸 개수만 보여 준다(예: 2 / 2).
- ★ 화면의 6가지와 F2의 6가지가 완전히 같지는 않다(화면에는 "사고 유형"·"내 신호"가 없다).
  AI 담당과 항목 이름을 맞출 때 이 표를 정본으로 삼는다.

## 3. 사건경위서

| 번호 | 기능 | 하는 일 | 들어오는 것 → 나가는 것 | 규칙·예외 | 확인 문장 | 화면 | 우선순위 |
|---|---|---|---|---|---|---|---|
| 3.1 | 초안 만들기 (AI) | 대화·분석 내용으로 경위서 초안을 쓴다 | [경위서 만들기] → 초안 카드(미리보기·장수) | 조건: 판정 완료 + 사고 날짜·장소 확보(없으면 질문 먼저). 구성: ①일시·장소 ②사고 경위 ③영상 분석 결과 ④주장 요지. 감정 표현 없이 객관 서술. **축소(9/3)** — 반영 대화 수·쟁점 캡션을 뺀다 | 판정 완료 후 버튼을 누르면 4개 부분이 채워진 초안이 보인다 | S4-A | P0 |
| 3.2 | 고치기(다시 쓰기) | 고칠 곳을 적어 주면 경위서를 다시 쓴다 | [다시 쓰기] (+ 요청 글, 없어도 됨) → 버전이 하나 오른 새 초안 카드 | **축소(9/4)** — 진행 상태 화면(h31)은 만들지 않는다. 다시 쓰는 동안 단추를 잠가서(색 교체) 알린다. 대화는 앞으로만 가므로 새 버전 카드가 아래에 하나 더 붙는다 | [다시 쓰기]를 누르면 두 번째 버전 카드가 생긴다 | S4-A · S5 | P1 |
| 3.3 | PDF 받기 | 경위서를 PDF 파일로 내려받는다 | [PDF 받기] → 파일 (사건경위서_사건제목_날짜.pdf) | **축소(9/3)** — 실패 화면(h32)은 만들지 않는다. 단계 완료 기준은 초안 생성(3.1)이며 PDF는 반복 가능한 행동 | 받은 PDF를 열면 화면과 같은 내용이 담겨 있다 | S5 | P0 |

## 4. 반박의견서

| 번호 | 기능 | 하는 일 | 들어오는 것 → 나가는 것 | 규칙·예외 | 확인 문장 | 화면 | 우선순위 |
|---|---|---|---|---|---|---|---|
| 4.1 | 초안 만들기 (AI) | 담당자에게 보낼 반박의견서를 쓴다 | 조건 충족 → 초안 카드(받는이·제목·본문·첨부) | 조건: 판정 있음 + 경위서 있음. 안 되면 버튼 잠금 + "경위서를 만들면 열려요". 제목 자동: "과실비율 재검토 요청 (접수번호 ○○)". **접수번호는 필수로 바꿨다 (8/27)** — 접수번호가 없으면 보험사 담당자가 어느 사건인지 못 찾아 회신 자체가 오지 않는다. 받는이와 접수번호 둘 다 채워져야 보내기가 열린다. ★팀 확인 필요. 첨부 기본: 경위서 PDF + 영상(뺄 수 있음) | 경위서가 없으면 반박의견서 버튼이 잠겨 있고 이유가 보인다 | S4-A | P0 |
| 4.2 | 고치기 | 받는이·제목·본문·첨부를 수정한다 | 수정 → 초안 갱신 | 받는이는 필수 입력(비워 두면 보내기 잠금) | 고친 내용이 발송 확인 팝업에 그대로 나온다 | S6 | P0 |
| 4.3 | 보내기 | 확인 후 이메일로 발송한다 | [보내기] → 형식 검사 → 확인 팝업(P-3) → 발송 | 받는이가 이메일 형식이 아니면 그 자리에서 표시. 팝업에 받는이·첨부·"보낸 뒤 취소 불가" 안내. 실패 시 안내+재시도. ★ 시연은 진짜 발송/모의 발송 중 팀 결정 | 받는이가 이메일 형식이 아니면 보내기 전에 바로 알 수 있다 | S6 | P0 |
| 4.4 | 발송 기록 | 보낸 결과를 남긴다 | 성공 → 발송 완료 카드(시각·받는이), 단계 완료 | 발송된 문서는 수정 불가. 다시 보내려면 새 문서로 | 발송 후 현황판에 "발송 완료 ○월 ○일 ○시"가 남는다 | S4-A·B | P0 |

## 5. 진행 상태와 이력

| 번호 | 기능 | 하는 일 | 들어오는 것 → 나가는 것 | 규칙·예외 | 확인 문장 | 화면 | 우선순위 |
|---|---|---|---|---|---|---|---|
| 5.1 | 진행 단계 표시 | 4단계×3상태를 보여 준다 | 사건 상태 → 단계 표시(현황판 상단, 휴대폰 띠) | 단계 정의는 0.6. 완료 기준: 분석=**분석 요약 도착**(9/3), 과실비율=판정 카드 도착, 경위서=초안 생성, 반박의견서=발송 성공 | 판정이 끝나면 "과실비율" 단계가 완료로 바뀐다 | S4-B | P0 |
| ~~5.2~~ | ~~변경 이력~~ | — | — | **제외(9/3)** — 보기 화면(h39)뿐 아니라 기록도 하지 않는다 | — | — | **제외** |
| ~~5.3~~ | ~~상태 배너~~ | — | — | **제외(9/3)** — 재판정이 빠져서 알릴 것이 없다 | — | — | **제외** |

## 6. 로그인 (범위 ★ — 마인드맵에 없고 유저플로우에는 있음)

| 번호 | 기능 | 하는 일 | 규칙·예외 | 확인 문장 | 화면 | 우선순위 |
|---|---|---|---|---|---|---|
| 6.1 | 로그인 | 이메일+비밀번호로 들어온다 | 실패: "이메일 또는 비밀번호가 맞지 않아요" | 틀린 값을 넣으면 이유가 보이고 다시 입력할 수 있다 | S1 | P1 |
| 6.2 | 회원가입 | 계정을 만든다 | 이메일 형식·중복·비밀번호 규칙 검사. 성공하면 바로 로그인 상태. 필수 동의 3가지(이용약관·개인정보·블랙박스 영상) 체크 전에는 [가입하기] 잠금. 각 동의의 [보기] → P-6 팝업(읽기 전용, 동의는 가입 화면에서만) | 가입 직후 다시 로그인하지 않고 사건 목록이 보인다 | S2 | P1 |
| 6.3 | 심사위원 대비 | 웹 주소를 열면 바로 써 볼 수 있게 한다 | 방법 ★택1: 체험 계정 자동 로그인 / 로그인 없이 브라우저별 분리. 요강상 접속 불가면 결격이므로 이것만은 필수 | 아무 준비 없이 주소만 열어도 새 사건을 만들 수 있다 | 전체 | **P0** |

---

## 화면 ↔ 기능 짝표

| 화면 | 담는 기능 |
|---|---|
| S0 첫 화면 | (소개 글, 시작 버튼 — 기능 없음) |
| S1·S2 로그인·회원가입 | 6.1, 6.2 |
| S3 사건 목록 | 1.5 |
| S4-A 채팅 | 1.1~1.4, 2.1 · 2.3 · 2.4 · 2.5, 3.1, 4.1, 4.4 |
| S4-B 현황판 | 5.1 (예상 과실비율 · 진행 단계 · 서류 세 덩이뿐) |
| S5 경위서 전문 | 3.3 |
| S6 반박의견서 | 4.2, 4.3 |
| 팝업 | P-1: 2.5 / ~~P-2~~ / P-3: 4.3 / ~~P-4~~ / P-5: 4.3 발송 실패만 / P-6: 6.2(약관 보기) |

## 주고받는 데이터 요약 (프론트·백엔드·AI 공유용)

**화면 → 서버**: 사고 설명 글 / 영상 파일 / 질문에 대한 답(글) / 반박의견서 수정 내용(받는이·제목·본문·첨부 선택) / 발송 요청

**서버(AI) → 화면**: 영상 분석 요약(글) / 모자란 것에 대한 질문(글) / 판정(비율·도표 이름·가감 목록·한 줄 결론) / 유사 사례(번호·요약) / 경위서(부분별 글) / 반박의견서 초안 / PDF 파일 / 사건 상태·단계

## 발견한 어긋남 — 팀 확인 필요 ★

| # | 어긋남 | 제안 |
|---|---|---|
| 1 | 시안에서 같은 사건의 비율이 채팅 100:0, 결과 카드 0:100 | 0.1 규칙("나 ○ : 상대 ○" 고정)으로 통일 |
| 2 | 로그인이 유저플로우에는 있고 기능명세서 마인드맵에는 없음 | 6번으로 정리. P1로 하되 6.3(심사위원 접속)은 P0 |
| 3 | "영상 보관함"이 화면 시안에만 있음 (PRD·플로우·명세에 없음) | 이번 범위 제외 |
| 4 | 8/23 기획 "영상은 분석 후 저장 안 함" vs PRD의 재판정·메일 첨부(영상 필요) | 0.4처럼 "사건 안에서만 보관·사용"으로 문구 변경 |
| 5 | 시연 사례가 두 갈래: 8/23 급정거 추돌(나 0:상대 100) vs 시안 교차로 이륜차 신호위반 | AI 담당이 영상+뒤집힌 사례 3건을 확보할 수 있는 쪽으로 결정. 화면 구조는 어느 쪽이든 같음 |
| 6 | 플로우의 "분석 시작 버튼" vs 시안의 채팅 자동 진행 | 자동 시작으로 통일 (1.4) |
| 7 | 반박의견서가 플로우에서 3곳에서 열림 | 조건 1개로 통일: 판정 있음 + 경위서 있음 (4.1) |
